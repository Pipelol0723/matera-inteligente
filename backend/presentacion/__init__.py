"""Capa de presentacion: controladores REST y conversion entre JSON y objetos del dominio.

Solo responde JSON (RA1). Valida la forma de la peticion en el borde y le
entrega a la aplicacion objetos del dominio, nunca un request ni un dict (RA6).
"""
